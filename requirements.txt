# -*- coding: utf-8 -*-
# NOTE: keep this file ASCII-only below the encoding line above.
# pip decodes requirements files with locale.getpreferredencoding() unless a
# PEP 263 header is present; hot update (services/upgrade.py) reinstalls from
# this file on the user's machine, where the locale may be cp932/gbk/etc.
fastapi~=0.115.0
uvicorn[standard]~=0.32.0
pydantic~=2.9.0
SQLAlchemy~=2.0.36
psycopg[binary]~=3.2.3
redis~=5.2.0
python-dotenv~=1.0.1
httpx[socks]~=0.28.1
# Site scraping goes through pyquery (lxml under the hood)
pyquery~=2.0.0
lxml~=5.3.0
apscheduler~=3.10.4
# Realtime file events for watched dirs; without it the feature degrades to
# periodic full reconciliation, startup is unaffected
watchdog~=6.0.0
PyJWT~=2.9.0
bcrypt~=4.2.0
# Passkey (WebAuthn) signature verification
webauthn~=3.0.0
loguru~=0.7.3
torrentool~=1.2.0
xmltodict~=0.14.0
pycryptodome~=3.21.0

qbittorrent-api~=2024.11.70
transmission-rpc~=7.0.11
PlexAPI~=4.15.16
